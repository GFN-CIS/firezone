EXTERNAL_URL={{ server_url }}
ADMIN_EMAIL={{ firezone_admin }}
FZ_INSTALL_DIR={{ firezone_dir }}
DEFAULT_ADMIN_PASSWORD={{ lookup('community.general.random_string', base64=True, length=12) }}
GUARDIAN_SECRET_KEY={{ lookup('community.general.random_string', base64=True, length=48) }}
SECRET_KEY_BASE={{ lookup('community.general.random_string', base64=True, length=48) }}
LIVE_VIEW_SIGNING_SALT={{ lookup('community.general.random_string', base64=True, length=24) }}
COOKIE_SIGNING_SALT={{ lookup('community.general.random_string', base64=True, length=6) }}
COOKIE_ENCRYPTION_SALT={{ lookup('community.general.random_string', base64=True, length=6) }}
DATABASE_ENCRYPTION_KEY={{ lookup('community.general.random_string', base64=True, length=32) }}
DATABASE_PASSWORD={{ lookup('community.general.random_string', base64=True, length=12) }}
FZ_SUBNET={{ mgmt_subnet }}
FZ_MGR_IP={{ mgr_ip }}
WIREGUARD_IPV4_NETWORK={{ client_subnet }}
FZ_PORT={{ wireguard_port }}
WIREGUARD_PORT={{ wireguard_port }}
